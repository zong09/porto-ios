# Fonts

Drop the **Anuphan** family `.ttf` files here and register them via `INFOPLIST_KEY`/`UIAppFonts`
in `project.yml` (Wave 3). Not committed as binaries in Wave 0. Until then the app uses the system
font. Web app uses Anuphan for Thai/Latin parity.
